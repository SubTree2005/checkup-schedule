# 更新记录

## Unreleased

- 接入原生微信小程序患者端，覆盖账号、医院/套餐/项目选择、个人状态、体检路线、执行记录和院内导航。
- 新增患者 Bearer 会话和 `/api/patient` API，并保持与医院管理员会话隔离。
- 新增医院端套餐 CRUD 与上下架，患者小程序按医院动态读取已上架套餐，不再依赖前端写死数据。
- 统一医院数据上传为工作区 JSON 1.0，支持一次原子导入多楼层 GIS、科室、检查项目和多个套餐，并可按稳定业务 key 重复更新。
- 新增数据库到 Scheduler V10 的适配层，服务端根据科室时间窗、容量、队列和距离生成及重排路线。
- 增加患者端端到端 Backend 集成测试，并移除小程序内的明文密码与本地排序器。

## 0.2.0 - 2026-08-28

- 实现 FastAPI + SQLAlchemy Backend，覆盖需求说明书 15 张业务表。
- 增加医院管理员注册、登录会话、PBKDF2 密码散列和多医院数据隔离。
- 实现科室、检查项目、GIS、异常、排队快照和首页看板 API。
- 实现医院 Web 管理端、人流 GIS 渲染、基础信息维护与临时调整流程。
- 增加微信云托管 Dockerfile、MySQL 环境配置文档和 Backend 集成测试。

## V10

- 新增长耗时项目提前软奖励，奖励随日程推进增强，降低大项目拖到当天后段后无法完成的风险。
- 新增 `Exam.is_critical` 静态关键标记；它与原有动态 critical-path 风险并存，不替代前置关系和截止时间传播。
- 个人移动画像增加老/中/青 × 男/女六类初始速度、当前速度、置信度、累计距离、累计行程和画像版本；可靠距离与步行时间通过稳健微批和 EWMA 更新速度，缺少距离时兼容原 factor 路径。
- 新增慢速患者长距离及跨楼层惩罚，优先选择同层或较短路线，但不放宽检查时间窗等硬约束。
- 新增 `MedicalEligibilityRule` 规则接口，供后续接入空腹、禁忌等业务规则；含自定义规则时 Hybrid 明确使用启发式，防止 CP-SAT 绕过未翻译的规则。

## V9

- 整理发布版时补充本地复现说明、合理的 `.gitignore`，并让正式 `rolling_cp_sat` / `feedback_cp_sat` 重复实验在缺少 OR-Tools 时硬失败；普通生产调度的启发式 fallback 行为不变。
- 新增独立 `critical_path.py`：支持多级 prerequisite DAG、多 successor、循环检测和下游截止时间反向传播；普通叶子任务保持自身截止时间。
- 批量启发式增加 `effective_latest_finish`、`critical_slack`、非线性 `completion_risk` 和 terminal pressure；高风险保护窗口按剩余任务/容量自适应。
- 冻结、进行中、完成任务继续遵守 V8 规则；关键路径只改变未冻结候选的优先层，不放宽任何硬时间窗。
- CP-SAT 增加显式 prerequisite 约束、关键截止大间隔目标、critical/resource-conflict 邻域选择和完整求解统计；失败可靠回退启发式。
- 正式增加 no/wait/activity/dual 四组反馈消融，以及 rolling/feedback 的 heuristic/CP-SAT 策略入口。
- 等待反馈增加 configurable update cap、最小批量和 residual shadow guard；个人活动模型增加个人级 factor update cap，手机加速度适配接口保持不变。
- 增加 8 类压力场景与 ±10/20/30% predictor bias；Ground Truth 故障、服务变慢、迟到和中断不泄漏给策略。
- 增加 full completion、unfinished、terminal completion、critical miss、高风险患者、改序通知、缺失项目分类和 CP-SAT 状态指标。
- 主实验复用 V8 原 10 seeds × 200 人。推荐 V9 无反馈：完成率 75.85%，mean wait 110.39 min；P90/P95 总历时点估计仍高约 4.2 min，未完全达到尾部目标。

## V8

- 将真实到达、真实服务耗时、真实步行耗时、依从延迟和资源故障移入独立不可变 `SimulationGroundTruth`；可观察患者输入中不再包含隐藏变量。
- Ground Truth 在策略创建前预采样并绑定场景指纹；调度决策方法增加无 Ground Truth 引用回归测试。
- 对照策略扩展为固定顺序 FCFS、最短可见队列贪心、V2 静态批量启发式、滚动无反馈和完整 V6。
- 新增 common-random-numbers 配对设计；默认运行 10 个独立种子，每个种子所有策略共享同一 Ground Truth。
- 新增均值、标准差、P50/P90/P95、Student-t 95% CI、逐种子配对效应和治疗策略胜率。
- 输出全部重复实验逐患者摘要，并保留代表性种子的五策略逐事件完整轨迹与 Ground Truth 审计表。

## V6

- 修正反馈语义：实际等待属于科室级全局反馈；实际活动时间表示患者个人移动耗时，不再更新科室服务时间。
- 新增 `AdaptivePersonalActivityPredictor`，按患者维护移动时间系数，未达到个人样本门槛时使用院内基准路程时间。
- 新增 `PersonalActivityFeedbackController`，按患者分别缓冲，使用硬边界、MAD/IQR、批次中位数和 EWMA 抑制单次行程波动。
- 新增 `PersonalActivityPrediction` 契约；精确排序、批量启发式、Rolling Horizon 和 CP-SAT 均可使用每位患者不同的路程时间。
- 新增 `AccelerometerSample`、`AccelerometerBatch` 和 `ActivitySensorAdapter`，为微信小程序、原生 App 或可穿戴设备预留平台无关接口。
- 科室等待控制器更名为 `GlobalWaitFeedbackController`，只接受 `WaitTimingFeedback`；保留 V5 名称别名但不再接受活动耗时字段。
- 个人活动模型支持状态导出恢复；传感器适配结果需通过患者、起终点、时间序列、置信度和事件幂等校验。
- 新增 10 项个人活动、传感器、个性化调度和状态持久化测试，并重写等待反馈边界测试。

## V5

- 新增独立 `feedback.py` 反馈适配层；调度器仍只读取 `WaitPrediction`，不接触用户反馈和预测器训练接口。
- 新增 `PatientTimingFeedback`，同时支持实际等待时间和实际检查/活动占用时间回传。
- 用户反馈按科室分别缓冲，默认至少累计 5 条才更新模型，单条反馈不会改变预测。
- 更新前使用中位数、MAD 和 IQR 对异常值做 Winsorize 截断，并对明显不合理的时长设置硬边界。
- 实际活动耗时以批次级 EWMA 渐进更新；实际等待同时修正均值偏差和 p90 覆盖误差。
- 反馈事件支持 ID 去重；控制器用锁串行化同进程并发更新，V4 预测器状态可直接迁移。
- 新增 12 项反馈解耦、异常值、去重、渐进更新和状态迁移测试。

## V4

- 将等待预测从“总工作量÷容量”升级为逐设备 FCFS 工作量模拟，正确处理多设备剩余时间不均衡和空闲设备。
- 快照可选传入队列中各项目的已知耗时，以及清洁、准备等固定运行延迟。
- 按“科室 × 工作日/周末 × 时段”在线学习检查耗时，并用科室全局统计对小样本时段收缩。
- 使用固定随机种子的蒙特卡洛仿真生成可复现 p90，并按实际等待结果校准欠覆盖。
- 新增预测状态导出/恢复、MAE/RMSE/偏差/p90 覆盖率回放指标和 10 项专项测试。
- 预测与排序、Rolling Horizon、CP-SAT 仍通过 `WaitPrediction` 接口解耦。

## V3

- 新增独立等待时间预测接口、在线 EWMA/排队工作量基线及 mean/p90 风险输出。
- 新增显式 Rolling Horizon 控制器、固定重排节拍和事件强制触发。
- 新增启发式保底 + CP-SAT 近端邻域优化；缺少依赖、超时或失败自动降级。
- CP-SAT 只优化近期开始时间和设备分配，保留启发式路线，降低实时求解风险。
- 新增预测过期保护、安全缓冲、求解时限、邻域大小和线程数配置。

## V2

- 新增患者可用时间段，可表示吃饭、外出和临时离院。
- 新增科室服务时间段，检查不得跨越午休或下班时间。
- 新增检查项目自身允许时间段、最早开始和最晚完成限制。
- 新增科室并行容量和资源预约日历，避免多名患者占用同一设备。
- 新增面向 100–200 名患者的批量启发式调度，移除大规模场景的阶乘搜索瓶颈。
- 新增滚动重排和近期计划冻结；硬约束变化优先于冻结。
- 保留 V1 精确算法及原有接口和测试。
# V7

- 新增 12 科室、200 患者的一日合成场景生成器。
- 新增一分钟粒度离散事件仿真，覆盖迟到、个人空闲时段、真实步速、实际服务耗时、排队、午休和设备停机。
- 在真实执行层接入 V6 的滚动调度、科室级等待反馈和患者级活动反馈。
- 新增固定医学顺序 + FCFS 对照组，两种策略共享同一隐藏真实样本。
- 新增逐患者 CSV/JSONL 轨迹、科室指标、重排日志、预测回放和中文评估报告。
